<!DOCTYPE html>
<html class="no-js" lang="en">

<head>
    <meta charset="utf-8">
    <title>Gadget Verse</title>
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta property="og:title" content="">
    <meta property="og:type" content="">
    <meta property="og:url" content="">
    <meta property="og:image" content="">
    <!-- Favicon -->
    <link rel="shortcut icon" type="image/x-icon" href="/evara-user/assets/imgs/theme/favicon.svg">
    <!-- Template CSS -->
    <link rel="stylesheet" href="/evara-user/assets/css/main.css?v=3.4">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

    
</head>

<style>
    .header__nav__option{
        font-size: 16px;
        color: #333;
        margin-right: 15px;
        text-decoration: none;
        font-weight: 600;
    }

    .header__nav__option{
        color:#ff7f50
    }

   

    .header {
        position: relative;
        z-index: 500;
    }

    .header-info {
    display: flex;
    justify-content: flex-end;
    align-items: center;
}

.user-menu {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    align-items: center;
}

.user-menu li {
    position: relative;
    margin-left: 15px;
}

.sign-in-link {
    text-decoration: none;
    color: #333;
    font-weight: 500;
    cursor: pointer;
}

.sign-in-link:hover {
    color: #007bff;
}

/* Dropdown menu styles */
.dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    min-width: 150px;
    background: #fff;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    border-radius: 4px;
    display: none;
    z-index: 1000;
    padding: 10px 0;
}

.dropdown-menu li {
    display: block;
    padding: 10px 15px;
    white-space: nowrap;
}

.dropdown-menu li a {
    text-decoration: none;
    color: #333;
    display: flex;
    align-items: center;
    gap: 8px;
}

.dropdown-menu li a:hover {
    background: #f8f9fa;
    color: #007bff;
}

/* Active dropdown menu */
.dropdown:hover .dropdown-menu {
    display: block;
}

/* Custom icon styles */
.fi-rs-user,
.fi-rs-user-circle,
.fi-rs-sign-out,
.fi-rs-user-plus {
    font-size: 1.2rem;
}

/* Ensure the form width is responsive */
.login_wrap {
    width: 100%; /* Ensures it takes full width inside the column */
    max-width: 500px; /* Sets a maximum width for better UI */
    margin: auto; /* Centers the element horizontally */
}




/* Full-page styling for the container */
/* Full-page styling for the container */
.full-page {
    display: flex; /* Enable flexbox */
    justify-content: center; /* Center horizontally */
    align-items: center; /* Center vertically */
    height: 100vh; /* Full viewport height */
}

/* Ensure the login box retains its original size but is a bit wider */
.login_wrap {
    max-width: 500px; /* Increase width to make it longer */
    width: 100%; /* Ensure it adapts responsively */
    padding: 30px; /* Add padding for a cleaner layout */
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Optional: subtle shadow for depth */
}

/* Toast Styles */
.toast {
        position: fixed;
        top: 20px;
        right: 20px;
        background-color: #e74c3c;
        color: white;
        padding: 15px;
        border-radius: 5px;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        opacity: 0;
        transform: translateY(-20px);
        transition: opacity 0.5s ease, transform 0.5s ease;
      }

      .toast.show {
        opacity: 1;
        transform: translateY(0);
      }

      .toast.success {
        background-color: #2ecc71;
      }

      .toast.error {
        background-color: #e74c3c;
      }

      .text-muted {
        position: relative;
        text-decoration: none;
        color: #6c757d; /* Muted text color */
        font-weight: 500;
        display: inline-block; /* Ensures the pseudo-element aligns correctly */
    }

    .text-muted:hover {
        color: #007bff; /* Change text color on hover */
    }

    .text-muted::before {
        content: '';
        position: absolute;
        bottom: -2px; /* Distance from the text */
        left: 50%; /* Start from the center */
        width: 0;
        height: 2px;
        background-color: #007bff; /* Line color */
        transition: all 0.3s ease; /* Smooth animation */
        transform: translateX(-50%); /* Center-align the pseudo-element */
    }

    .text-muted:hover::before {
        width: 100%; /* Expand to full width on hover */
    }

    #otpTimer {
        font-size: 1.5rem; /* Increase the font size for better visibility */
        font-weight: bold; /* Make the text bold */
        color: #ffffff; /* White text for contrast */
        text-align: center; /* Center the text */
        padding: 10px 20px; /* Add some padding around the timer */
        border-radius: 25px; /* Rounded corners for a modern look */
        margin-top: 15px; /* Space above the timer */
        display: inline-block; /* Align properly within the layout */
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Subtle shadow for a floating effect */
    }

    .login_wrap {
        display: flex; /* Use Flexbox for centering */
        flex-direction: column; /* Stack elements vertically */
        align-items: center; /* Center child elements horizontally */
        justify-content: center; /* Optional: Center child elements vertically */
        text-align: center; /* Center text-based elements */
    }



</style>

<body>
    
    <header class="header-area header-style-1 header-height-2">
        <div class="header-middle header-middle-ptb-1 d-none d-lg-block">
            <div class="container">
                <div class="header-wrap">
                    <div class="logo logo-width-1">
                        <a href="/"><img src="/evara-user/assets/imgs/theme/logo.svg" alt="logo"></a>
                    </div>
                </div>
            </div>
        </div>
        <div class="header-bottom header-bottom-bg-color sticky-bar">
            <div class="container">
                <div class="header-wrap header-space-between position-relative">
                    <div class="logo logo-width-1 d-block d-lg-none">
                        <a href="/"><img src="/evara-user/assets/imgs/theme/logo.svg" alt="logo"></a>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <script>
        function toggleDropdown() {
            const dropdown = document.getElementById('dropdownContent');
            dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
            document.addEventListener('click', (e) => {
                if (!e.target.closest('.dropdown')) {
                    dropdown.style.display = 'none';
                }
            });
        }
    </script>
<!--header ends-->
    <main class="main">
        <div class="page-header breadcrumb-wrap">
            <div class="container">
                <div class="breadcrumb">
                    <a href="/" rel="nofollow">Home</a>
                    <span></span> Login
                    
                </div>
            </div>
        </div>
        <section class="pt-50 pb-50">
            
            <div class="col-lg-12">
                <div id="toast" class="toast"></div>
                <!-- Fullscreen Flexbox Container -->
                <div class="d-flex justify-content-center align-items-center vh-100">
                    <!-- Centered Card -->
                    <div class="login_wrap widget-taber-content p-30 background-white border-radius-10">
                        <div class="padding_eight_all bg-white">
                            <!-- Heading -->
                            <div class="heading_s1">
                                <h3 class="mb-30 text-center">Email Verification</h3>
                            </div>
                            <!-- Form -->
                            <form  onsubmit="return validateOtpForm()" >
                                <div class="form-group">
                                    <input type="text" required id="otp" name="otp" class="form-control text-center" placeholder="Enter OTP">
                                </div>
                                <div class="form-group d-flex justify-content-center">
                                    <button type="submit" class="btn btn-fill-out hover-up" name="verifyOtp">Verify OTP</button>
                                </div>
                                <div class="otp-timer" id="otpTimer"></div>
                                <div class="divider-text-center mt-15 mb-15">
                                    <span> or</span>
                                </div>
                                <div class="form-group text-center">
                                    <button type="button" class="btn btn-outline-secondary" onclick="resendOtp()">Resend OTP</button>
                                </div>
                                <%if(locals.message && message.length > 0){%>
                                    <div class="alert alert-danger pt-25">
                                        <%=message%>
                                    </div>
                                <%}%>
                            </form>
                            <!-- Login Link -->
                            <div class="text-muted text-center mt-15">
                                Already verified? <a href="/login">Login Now</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
            <script>
                
                let otpTimerInterval
                let timer = 60

                function updateTimerColor(percentage) {
                    const timerElement = document.getElementById('otpTimer');
                    if (percentage > 50) {
                        timerElement.style.background = 'linear-gradient(90deg, #28a745, #34d058)';
                    } else if (percentage > 25) {
                        timerElement.style.background = 'linear-gradient(90deg, #ffc107, #ffd452)';
                    } else {
                        timerElement.style.background = 'linear-gradient(90deg, #dc3545, #e94d5a)';
                    }
                }


                function startOtpTimer(){
                    const timerElement = document.getElementById('otpTimer')
                    otpTimerInterval = setInterval(function() {
                        const minutes = Math.floor(timer/60)
                        const seconds = timer % 60
                        timerElement.textContent = `${minutes}:${seconds<10?'0':''}${seconds}`
                        updateTimerColor((timer/60)*100)
                        if(--timer < 0){
                            clearInterval(otpTimerInterval)
                            timerElement.textContent = 'Expired'
                            timerElement.style.backgroundColor = 'red'
                        }
                    }, 1000);
                }

                initializedOtpTimer()


                function initializedOtpTimer(){
                    clearInterval(otpTimerInterval)
                    timer = 60
                    startOtpTimer()
                }

                function validateOtpForm(){
                    const otpInput = document.getElementById('otp').value.trim()
                    $.ajax({
                        type:'POST',
                        url:'/verify-passForgot-otp',
                        data:{otp:otpInput},
                        success: function(response){
                            if(response.success){
                                Swal.fire({
                                    icon:'success',
                                    title:'OTP Verified Successfully',
                                    showConfirmButton:false,
                                    timer:1500
                                }).then(()=>{
                                    window.location.href = response.redirectUrl
                                })
                            }else{
                                Swal.fire({
                                    icon:'error',
                                    title:'Invalid OTP',
                                    text:response.message
                                })
                            }
                        },
                        error:function(){
                            Swal.fire({
                                icon:'error',
                                title:'Error',
                                text:'Failed to verify OTP. Please try again.'
                            })
                        }
                    })
                    return false
                }

                function resendOtp (){
                    clearInterval(otpTimerInterval)
                    timer = 60
                    startOtpTimer()
                    $.ajax({
                        type:'POST',
                        url:'/resend-forgot-otp',
                        success: function (response){
                            if(response.success){
                                Swal.fire({
                                    icon:'success',
                                    title:'Resend OTP Successful',
                                    showConfirmButton:false,
                                    timer:1500,
                                })
                            }else {
                                Swal.fire({
                                    icon:'error',
                                    title:'Error',
                                    text:'Failed to resend OTP, Please try again.'
                                })
                            }
                        },
                        error: function (){
                            Swal.fire({
                                icon:'error',
                                title:'Error',
                                text:'Failed to resend OTP, Please try again.'
                            })
                        }
                            
                    })
                }
            </script>
        </section>
    </main>
    <%- include("../../views/partials/user/footer") %>